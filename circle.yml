machine:
  java:
    version: oraclejdk8

dependencies:
  override:
    - mvn -DskipTests clean install dependency:resolve-plugins dependency:go-offline

test:
  override:
    - ./circle-parallel.sh:
        parallel: true
